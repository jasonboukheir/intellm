result
result-*
.direnv
.claude/settings.local.json
.claude/scheduled_tasks.lock

# model artifacts (large, regenerable)
output/

# package-manager locks for non-nix tools (flake.lock IS tracked)
package-lock.json
yarn.lock
Cargo.lock

# jujutsu workspace state (only relevant when colocated with .git)
.jj/
